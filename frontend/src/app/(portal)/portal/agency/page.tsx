import { RolePage } from "@/components/portal/role-page";

export default function AgencyCasesPage() {
  return (
    <RolePage
      title="Cases routed to you"
      lead="Citizen reports about safety that the Assembly has routed to your service. Each one is private: only you and any other service it was sent to can read it."
    />
  );
}
