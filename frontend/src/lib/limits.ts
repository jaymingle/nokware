// Field lengths shared by more than one form.

/** A free-text note on a document action, a resubmission, a case or an escalation. */
export const NOTE_MAX = 2000;

/** What staff write at a stage of a case. Shorter, because a resident reads it on their status page. */
export const CASE_NOTE_MAX = 500;

/** The public name shown with a voice or a petition signature. */
export const NAME_MAX = 80;
